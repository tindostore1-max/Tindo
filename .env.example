# Configuración de la base de datos (SQLite por defecto)
DATABASE_PATH=inefablestore.db

# Clave secreta de Flask
SECRET_KEY=tu_clave_secreta_muy_segura

# =============================
# Credenciales de Administrador
# =============================
# Estas variables crean/actualizan el usuario admin al iniciar la app.
# Úsalas SOLO en local. En producción (Render) configúralas como Environment Variables o Secrets.
ADMIN_EMAIL=admin@tudominio.com
ADMIN_PASSWORD=tu_contraseña_segura

# =============================
# SMTP (Correo de notificaciones)
# =============================
# MODO SIMPLE (recomendado con Gmail):
# - Habilita 2FA y crea una "Contraseña de aplicación" en tu cuenta Gmail.
# - SOLO necesitas definir estos dos valores:
SMTP_USER=tu_correo@gmail.com
SMTP_PASSWORD=tu_clave_app_sin_espacios

# Los demás parámetros se autoconfiguran para Gmail:
#   HOST = smtp.gmail.com
#   PORT = 587 (STARTTLS)
#   FROM = SMTP_USER (remitente)
#   TLS  = habilitado

# Opcional: descomenta si deseas personalizar
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_FROM=otro_remitente@tudominio.com
# SMTP_USE_TLS=1
# SMTP_USE_SSL=0


